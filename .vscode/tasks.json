{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet build",            
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/TaskList.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}